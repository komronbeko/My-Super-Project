<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<style>
    /* <!----- arrow link uchun yozilgan stillar, internetdan olingan holos, prosta still uchun iwlatildi ------!> */
    .arrow {
        position: relative;
        width: 30px;
        height: 30px;
        cursor: pointer;
        transition: 0.5s;
        overflow: hidden;
    }

    .arrow:hover {
        border: solid 2px blue;
        border-radius: 50%;
    }

    .arrow:after {
        position: absolute;
        display: block;
        content: "";
        color: blue;
        width: 20px;
        height: 15px;
        top: -1px;
        border-bottom: solid 2px;
        transform: translatex(4px);
    }

    .arrow:before {
        position: absolute;
        display: block;
        content: "";
        color: blue;
        width: 8px;
        height: 8px;
        border-top: solid 2px;
        border-left: solid 2px;
        top: 50%;
        left: 2px;
        transform-origin: 0% 0%;
        transform: rotatez(-45deg);

    }

    .arrow:hover:before {
        animation: aniArrow01 1s cubic-bezier(0, 0.6, 1, 0.4) infinite 0.5s;
    }

    .arrow:hover:after {
        animation: aniArrow02 1s cubic-bezier(0, 0.6, 1, 0.4) infinite 0.5s;
    }


    @keyframes aniArrow01 {
        0% {
            transform: rotatez(-45deg) translateY(30px) translateX(30px);
        }

        100% {
            transform: rotatez(-45deg) translateY(-35px) translateX(-35px);
        }
    }


    @keyframes aniArrow02 {
        0% {
            transform: translateX(45px);
        }

        100% {
            transform: translateX(-44px);
        }
    }

    @keyframes borderAni {
        0% {
            border: solid 2px blue;
        }

        100% {
            border: solid 2px blue;
            border-radius: 50%;
        }
    }
</style>

<body>
    <header>
        <div class="flex items-start justify-between gap-2 mt-5 mx-10">
            <div class="flex items-start gap-5">
                <img src="/api/<%= findUser.image%>" alt="user_image"
                class="rounded-full w-40 h-40 color-red">
                <div>
                    <h4 class="font-medium text-2xl text-green-700"><%= findUser.username%></h4>
                    <p class="font-medium text-blue-500">Posts <%= filteredPosts.length%></p>
                    <p class="font-medium text-violet-500 mb-12">Followers <%= findUser.subscribe.length%></p>
                    <form action="/api/subscribe/<%= findUser.id %>" method="post">
                        <button class="py-1 px-3 rounded font-medium text-md border border-solid border-red-600 <%= subscribeAssets?.state === "false" ? "bg-red-500 text-white" : "bg-white text-red-600"%>"><%= subscribeAssets?.state === "true" ? "subscribed" : "subscribe"%></button>
                    </form>
                </div>
            </div>
            <a href="/api/home"
                class="flex items-center gap-2 inline-flex items-center text-2xl font-bold text-blue-600 dark:text-blue-500 hover:underline">
                <div class="arrow"></div>
                Home Page
            </a>
        </div>
    </header>
    <div class="grid grid-cols-4 gap-2">
        <% filteredPosts.map((el)=>{%>
        <a href="/api/postOne/<%= el.id%>">
            <div
                class="mx-auto mt-11 w-[350px] transform overflow-hidden rounded-lg bg-white dark:bg-slate-800 shadow-md duration-300 hover:scale-105 hover:shadow-lg">
                <video  autoplay muted loop class="mx-auto mt-11 w-[350px] transform overflow-hidden rounded-lg bg-white dark:bg-slate-800 shadow-md duration-300 hover:scale-105 hover:shadow-lg" >
                    <source src="/api/<%= el.video %>" type="video/mp4">
                    Your browser does not support the video tag.
                </video>
                <div class="p-4">
                    <h2 class="mb-2 text-lg font-medium dark:text-white text-gray-900"><%= el.title %></h2>
                    <p class="mb-2 text-base dark:text-gray-300 text-gray-700"><%= el.description %></p>
                    <div class="flex items-center">
                        <p class="mr-2 text-lg font-semibold text-gray-900 dark:text-white"><%= el.place %></p>
                        <p class="ml-auto text-base font-medium text-green-500"><%= el.date %></p>
                    </div>
                </div>
            </div>
        </a>
        <% })%>
    </div>
</body>

</html>